/**
 * createElement - Creates an element with the given tag name and attributes. Uses
 * @param {*} tagName
 * @param {*} props
 * @param {*} children
 */
export function createElement(tagName, props, children) {}
